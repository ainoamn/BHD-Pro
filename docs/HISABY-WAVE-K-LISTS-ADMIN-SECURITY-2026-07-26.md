# Hisaby — موجة K: صدق قوائم إضافية + إدارة + أمن (26 يوليو 2026)

**الفرع:** `main` · بعد موجة J  
**المستودع:** [ainoamn/BHD-Pro](https://github.com/ainoamn/BHD-Pro)

---

## ما شُحن

### 1) صدق أخطاء التحميل (قوائم إضافية)
- إيصالات، مستندات نقدية، مشتريات، إشعارات تسليم
- مطالبات موظفين، التزامات، ضريبة القيمة المضافة، إقفال فترات
- جرد مخزون، رواتب موظفين
- الاشتراك (`/subscription`) — `QueryError` عند فشل التحميل

### 2) لوحة المنصة
- `/admin/tenants` — حالة تحميل + خطأ + إعادة محاولة
- `/admin/users` — نفس النمط (`loadError` + إعادة محاولة)

### 3) دخول وواجهة
- نصوص دخول المشرف عبر i18n (`adminLoginTitle` / `adminLoginHint` / …)
- نجاح الدفع: عرض `labelEn`/`labelAr` حسب اللغة
- صالة المطاعم (`/resto/board`) — زر إعادة محاولة عند فشل اللوحة الحية

### 4) أمن تشغيلي
- `@Throttle` على `PATCH /companies/me/security` (20/دقيقة)

---

## تحقق سريع

1. أوقف API وافتح `/receipts` أو `/vat` أو `/subscription` — يظهر خطأ صادق وزر إعادة.  
2. `/admin/tenants` و `/admin/users` — نفس السلوك.  
3. `/login` لمسار مشرف يعرض النصوص المترجمة.  
4. تعديل أمن الشركة بسرعة متكررة يُحدّ بعد الحد.

---

## متبقٍ

WAF · Sentry SDK كامل · OTA live · Capacitor · SoftPOS
