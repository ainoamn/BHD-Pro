# Hisaby — مقارنة الباقات في الصفحة الرئيسية

**التاريخ:** 26 يوليو 2026 · Asia/Muscat  
**الفرع:** `main` · المستودع: [ainoamn/BHD-Pro](https://github.com/ainoamn/BHD-Pro)  
**مرتبط بـ:** [`HISABY-ADMIN-PLANS-USERS-PRICING-2026-07-26.md`](./HISABY-ADMIN-PLANS-USERS-PRICING-2026-07-26.md)

---

## الهدف

تمكين العميل من مقارنة الفوارق بين الباقات مباشرة من قسم التسعير في الصفحة الرئيسية (`/#pricing`)، بجانب تبديل **شهري / سنوي**.

---

## الواجهة

| عنصر | السلوك |
|------|--------|
| زر **مقارنة** | داخل نفس مجموعة **شهري / سنوي** في `#pricing` (ظاهر للعميل) |
| إخفاء المقارنة | نفس الزر يغلق الجدول |
| الفوارق فقط (افتراضي) | صفوف تختلف قيمها بين الباقات |
| عرض الكل | كل الصفوف (سعر، حدود، خدمات) |
| تعطيل الزر | إذا وُجدت باقة واحدة فقط |
| ملاحظة داخلية للأسعار | **لا تُعرض** على الصفحة الرئيسية للعميل |

### أعمدة الجدول

اسم كل باقة نشطة من `GET /api/public/plans` (عربي/إنجليزي حسب لغة الصفحة).

### صفوف المقارنة

1. **السعر** — حسب وضع شهري أو سنوي الحالي  
2. **المستخدمون** / **الفواتير / شهر** / **الدعم**  
3. **الخدمات والصلاحيات** من `highlights` — ✓ مشمولة · ✕ غير مشمولة  

الصفوف التي تختلف بين الباقات تُميَّز بلون خفيف.

---

## الملفات

| ملف | دور |
|-----|-----|
| `frontend/src/components/landing/landing-page.tsx` | زر المقارنة + جدول `buildPlanCompareRows` |
| `frontend/src/lib/landing-copy.ts` | نصوص عربية/إنجليزية (`comparePlans`, `compareDiffsOnly`, …) |
| `backend` `publicPlans()` | مصدر البيانات الحي (`highlights`, أسعار، حدود) — بدون تغيير لهذا الزر |

---

## تحقق سريع

1. افتح `/` → انتقل إلى الباقات.  
2. تأكد من وجود **شهري** · **سنوي** · **مقارنة** في نفس الشريط.  
3. تأكد أن عبارة «الأسعار من لوحة الباقات…» **غير ظاهرة**.  
4. اضغط **مقارنة** — يظهر جدول الفوارق.  
5. بدّل إلى **عرض الكل** ثم ارجع إلى **الفوارق فقط**.  
6. بدّل شهري↔سنوي — يتحدّث صف السعر في الجدول.  
7. أضف/احذف باقة من `/admin/plans` وانتظر التحديث الحي — الأعمدة تتغير.

---

## ملاحظات منتج

- مقارنة **العميل** (الصفحة الرئيسية) تختلف عن مقارنة **المشرف** في `/admin/plans` (أعمدة تحرير كاملة).  
- المصدر واحد: كتالوج الباقات النشطة في قاعدة البيانات عبر API العام.
