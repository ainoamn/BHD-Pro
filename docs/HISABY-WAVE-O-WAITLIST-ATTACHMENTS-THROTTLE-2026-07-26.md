# Hisaby — موجة O: قائمة انتظار/حجوزات/مرفقات + throttles (26 يوليو 2026)

**الفرع:** `main` · بعد موجة N  
**المستودع:** [ainoamn/BHD-Pro](https://github.com/ainoamn/BHD-Pro)

---

## ما شُحن

### 1) صدق أخطاء التحميل
- مطاعم: قائمة انتظار + حجوزات — خطأ + إعادة محاولة
- مرفقات الكيانات — `isError` + retry (و`onError` للحذف)
- تاريخ ورديات POS — لا يُعرض كـ «لا سجل» عند فشل API
- `/pos/books` — لا يظهر upsell الربط أثناء التحميل/الفشل

### 2) أمن تشغيلي (`@Throttle`)
- إقفال/فتح فترات محاسبية
- ترقية اشتراك
- إلغاء/حذف مفاتيح API
- فتح/إغلاق وردية كاشير

### 3) i18n
- `common.retry` في ar/en

---

## تحقق سريع

1. أوقف API وافتح `/resto/waitlist` أو `/resto/reservations` — خطأ + إعادة.  
2. `/pos/books` أثناء فشل الشبكة لا يعرض «فعّل الربط».  
3. فتح وردية أو ترقية اشتراك بسرعة متكررة يُحدّ بعد الحد.

---

## متبقٍ

WAF · Sentry SDK كامل · OTA live · Capacitor · SoftPOS
