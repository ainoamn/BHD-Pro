# موجة BG — فرض مستودع الكاشير + اتساق الجلسة + إصلاحات POS

**التاريخ:** 27 يوليو 2026  
**بعد:** Waves BC → BF

## ما أُكمل

| البند | التفاصيل |
|-------|----------|
| API مستخدمين | إنشاء/تحديث CASHIER بدون `defaultWarehouseId` → `400` |
| واجهة مستخدمين | منع مسح مستودع الكاشير ومنع التحويل إلى كاشير بلا مستودع |
| تسجيل الدخول | `login` / `2FA` / `Google` يمرّرون `defaultWarehouse*` مثل `restoreSession` |
| POS إعادة المحاولة | نفس منطق القفل المنزلي عند Retry (لا يسقط على أول مستودع) |
| تسمية المستودع | عرض `homeWarehouse` من السياق حتى لو لم يكن في قائمة التجزئة |
| توثيق | اختبار دخان إضافي في قائمة الإطلاق |

## تذكير إنتاج

```bash
npx prisma migrate deploy
```

Migration: `20260727133000_user_home_warehouse_pos_fulfillment`
