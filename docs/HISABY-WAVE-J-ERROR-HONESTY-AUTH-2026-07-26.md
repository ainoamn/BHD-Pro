# Hisaby — موجة J: صدق الأخطاء + تحسين الدخول (26 يوليو 2026)

**الفرع:** `main` · بعد موجتي H و I  
**المستودع:** [ainoamn/BHD-Pro](https://github.com/ainoamn/BHD-Pro)

---

## ما شُحن

### 1) صدق أخطاء التحميل
- دليل التكاملات (readme) — لم يعد يدور للأبد عند الفشل
- إعدادات 2FA والحماية المزدوجة — `QueryError` بدل عرض حالة خاطئة
- قوائم: مستخدمون، يومية، جهات اتصال، مخزون، مفاتيح API، دفتر أستاذ، CRUD ERP
- تقارير مالية (كل تبويب) + بنوك التسوية

### 2) دخول / تسجيل
- رابط إنشاء حساب من صفحة الدخول
- `autoComplete` للبريد وكلمة المرور
- تسجيل: تحقق قوة كلمة المرور مطابق للخادم + أخطاء شبكة/فشل أوضح
- نص «العودة للرئيسية» عبر i18n

### 3) أمن تشغيلي
- `@Throttle` على `POST /messaging/test` (10/دقيقة)

### 4) نصوص وتوثيق
- تصحيح `sendDocumentHint` و `dualControl.futureNote`
- تحديث ملاحظة صالة المطاعم في خطة المطبخ

---

## تحقق سريع

1. أوقف API وافتح `/users` أو `/integrations` (اقرأني) — زر إعادة محاولة.  
2. `/login` يظهر رابط التسجيل.  
3. تسجيل بكلمة ضعيفة يُرفض محلياً برسالة الـ hint.  
4. اختبار رسالة من التكاملات بسرعة متكررة يُحدّ بعد الحد.

---

## متبقٍ

WAF · Sentry SDK كامل · OTA live · Capacitor · SoftPOS
