# Hisaby — موجة Q: لوحات الربط / حجز عام / قائمة صالة (26 يوليو 2026)

**الفرع:** `main` · بعد موجة P  
**المستودع:** [ainoamn/BHD-Pro](https://github.com/ainoamn/BHD-Pro)

---

## ما شُحن

### 1) صدق أخطاء التحميل
- لوحة تطبيقات لوحة التحكم + hub الربط — فشل API يبقى `null` (تحميل) لا «غير مربوط»
- بحث جهات الاتصال — رسالة فشل بحث بدل «—»
- حجز عام `/reserve/[slug]` — خطأ أوقات + إعادة محاولة
- قائمة أصناف صالة المطاعم — خطأ + إعادة عند فشل القائمة

### 2) UX
- صفحة دفع فاتورة عامة — اسم البوابة حسب اللغة (`nameEn`/`nameAr`)

### 3) أمن تشغيلي (`@Throttle`)
- مطاعم: void / close / pay-link / cancel
- جهات اتصال: تعديل رصيد المتجر

---

## تحقق سريع

1. أوقف API وافتح لوحة التطبيقات — تظهر «…» لا «غير مربوط».  
2. `/reserve/…` مع API متوقف — خطأ أوقات + إعادة.  
3. إغلاق طلب مطعم أو تعديل رصيد متجر بسرعة متكررة يُحدّ بعد الحد.

---

## متبقٍ

WAF · Sentry SDK كامل · OTA live · Capacitor · SoftPOS
